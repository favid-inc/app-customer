import React from 'react';
import { Button } from '@kitten/ui';

export const CancelOrderButton: React.FunctionComponent<null> = () => {
  return (
    <Button size='medium'>
      Cancelar Pedido
    </Button>
  );
};
